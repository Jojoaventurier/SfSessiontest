{% extends 'base.html.twig' %}

{% block title %}Liste des sessions{% endblock %}

{% block body %}
<div class="wrapper centered">

    <h1>Liste des sessions</h1>

    <div class="eighty">  
        <table class="center">
            <tr>
                <th class="fivehundred">Intitulé Session</th>
                <th class="small">Date de début</th>
                <th class="small">Date de fin</th>
                <th class="small">Nb total</th>
                <th class="small">Nb réservées</th>
                <th class="small">Nb restantes</th>
                <th class="small">Détail programme</th>
            </tr>
            {% for session in sessions %}
            <tr>
                <td><a href="{{ path('show_session', {'id': session.id}) }}">{{ session }}</a></td>
                <td>{{ session.startDate | date("m/d/Y") }}</td>
                <td>{{ session.endDate | date("m/d/Y") }}</td>
                <td>{{ session.totalNumber }}</td>
                <td>{{ session.numberBooked}}</td>
                <td>{{ session.numberLeft }}</td>
                <td>Détail</td>
            </tr>
            {% endfor %}
        </table>

    </div> 
    {% if is_granted('ROLE_ADMIN')  %}
    <div class="btnDiv btnSize wrapper">
        <a class="btn" href="{{ path('new_session') }}">Ajouter une session</a>
    </div>
    {% endif %}
      
</div>
{% endblock %}
